/* This file was generated by SableCC (http://www.sablecc.org/). */

package prolixa.node;

import java.util.*;
import prolixa.analysis.*;

@SuppressWarnings("nls")
public final class AABlocoAComando extends PAComando
{
    private final LinkedList<PADeclaracao> _declaracoes_ = new LinkedList<PADeclaracao>();
    private final LinkedList<PAComando> _comandos_ = new LinkedList<PAComando>();
    private PAComando _elseCmd_;

    public AABlocoAComando()
    {
        // Constructor
    }

    public AABlocoAComando(
        @SuppressWarnings("hiding") List<?> _declaracoes_,
        @SuppressWarnings("hiding") List<?> _comandos_,
        @SuppressWarnings("hiding") PAComando _elseCmd_)
    {
        // Constructor
        setDeclaracoes(_declaracoes_);

        setComandos(_comandos_);

        setElseCmd(_elseCmd_);

    }

    @Override
    public Object clone()
    {
        return new AABlocoAComando(
            cloneList(this._declaracoes_),
            cloneList(this._comandos_),
            cloneNode(this._elseCmd_));
    }

    @Override
    public void apply(Switch sw)
    {
        ((Analysis) sw).caseAABlocoAComando(this);
    }

    public LinkedList<PADeclaracao> getDeclaracoes()
    {
        return this._declaracoes_;
    }

    public void setDeclaracoes(List<?> list)
    {
        for(PADeclaracao e : this._declaracoes_)
        {
            e.parent(null);
        }
        this._declaracoes_.clear();

        for(Object obj_e : list)
        {
            PADeclaracao e = (PADeclaracao) obj_e;
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
            this._declaracoes_.add(e);
        }
    }

    public LinkedList<PAComando> getComandos()
    {
        return this._comandos_;
    }

    public void setComandos(List<?> list)
    {
        for(PAComando e : this._comandos_)
        {
            e.parent(null);
        }
        this._comandos_.clear();

        for(Object obj_e : list)
        {
            PAComando e = (PAComando) obj_e;
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
            this._comandos_.add(e);
        }
    }

    public PAComando getElseCmd()
    {
        return this._elseCmd_;
    }

    public void setElseCmd(PAComando node)
    {
        if(this._elseCmd_ != null)
        {
            this._elseCmd_.parent(null);
        }

        if(node != null)
        {
            if(node.parent() != null)
            {
                node.parent().removeChild(node);
            }

            node.parent(this);
        }

        this._elseCmd_ = node;
    }

    @Override
    public String toString()
    {
        return ""
            + toString(this._declaracoes_)
            + toString(this._comandos_)
            + toString(this._elseCmd_);
    }

    @Override
    void removeChild(@SuppressWarnings("unused") Node child)
    {
        // Remove child
        if(this._declaracoes_.remove(child))
        {
            return;
        }

        if(this._comandos_.remove(child))
        {
            return;
        }

        if(this._elseCmd_ == child)
        {
            this._elseCmd_ = null;
            return;
        }

        throw new RuntimeException("Not a child.");
    }

    @Override
    void replaceChild(@SuppressWarnings("unused") Node oldChild, @SuppressWarnings("unused") Node newChild)
    {
        // Replace child
        for(ListIterator<PADeclaracao> i = this._declaracoes_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((PADeclaracao) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        for(ListIterator<PAComando> i = this._comandos_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((PAComando) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        if(this._elseCmd_ == oldChild)
        {
            setElseCmd((PAComando) newChild);
            return;
        }

        throw new RuntimeException("Not a child.");
    }
}
