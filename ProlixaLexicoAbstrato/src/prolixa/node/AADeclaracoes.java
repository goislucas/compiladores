/* This file was generated by SableCC (http://www.sablecc.org/). */

package prolixa.node;

import java.util.*;
import prolixa.analysis.*;

@SuppressWarnings("nls")
public final class AADeclaracoes extends PADeclaracoes
{
    private final LinkedList<PADeclaracao> _declaracoes_ = new LinkedList<PADeclaracao>();

    public AADeclaracoes()
    {
        // Constructor
    }

    public AADeclaracoes(
        @SuppressWarnings("hiding") List<?> _declaracoes_)
    {
        // Constructor
        setDeclaracoes(_declaracoes_);

    }

    @Override
    public Object clone()
    {
        return new AADeclaracoes(
            cloneList(this._declaracoes_));
    }

    @Override
    public void apply(Switch sw)
    {
        ((Analysis) sw).caseAADeclaracoes(this);
    }

    public LinkedList<PADeclaracao> getDeclaracoes()
    {
        return this._declaracoes_;
    }

    public void setDeclaracoes(List<?> list)
    {
        for(PADeclaracao e : this._declaracoes_)
        {
            e.parent(null);
        }
        this._declaracoes_.clear();

        for(Object obj_e : list)
        {
            PADeclaracao e = (PADeclaracao) obj_e;
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
            this._declaracoes_.add(e);
        }
    }

    @Override
    public String toString()
    {
        return ""
            + toString(this._declaracoes_);
    }

    @Override
    void removeChild(@SuppressWarnings("unused") Node child)
    {
        // Remove child
        if(this._declaracoes_.remove(child))
        {
            return;
        }

        throw new RuntimeException("Not a child.");
    }

    @Override
    void replaceChild(@SuppressWarnings("unused") Node oldChild, @SuppressWarnings("unused") Node newChild)
    {
        // Replace child
        for(ListIterator<PADeclaracao> i = this._declaracoes_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((PADeclaracao) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        throw new RuntimeException("Not a child.");
    }
}
